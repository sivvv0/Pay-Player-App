{
  "name": "pay-player",
  "version": "1.0.0",
  "description": "Pay-Player App",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Build your web app here'",
    "sync": "npx cap sync",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
